name: Create Release Branch from Main

on:
  workflow_dispatch:
  push:
    branches:
    - 'main'

jobs:
  package:
    uses: lsc-sde/lsc-sde/.github/workflows/flux-release.yaml@main
    with:
      directory: iac/flux/analytics-workspace-management
      yamlPath: analytics_workspace_management_branch
    secrets: inherit
